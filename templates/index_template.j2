<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MenuDB</title>
    <link rel="stylesheet" type="text/css" href="static/style.css">
</head>

<body>
    <header class="header">
        <h1>Digital Restaurant Menus</h1>
    </header>
    <div class="container">
        <ul>
            {% for yaml_dict in data | sort(attribute='restaurant.country_code,restaurant.city,restaurant.name') %}
            <li><a href="{{ yaml_dict._output_filename }}">{{ yaml_dict.restaurant.name }} ({{ yaml_dict.restaurant.city
                    }}, {{ yaml_dict.restaurant.country_code }})</a> - <small>{{ yaml_dict.restaurant.tags }}</small>
            </li>
            {% endfor %}
        </ul>
    </div>
    <footer class="footer">
        <p><small>Generated by MenuDB—a FoodTBD project</small></p>
    </footer>
</body>

</html>